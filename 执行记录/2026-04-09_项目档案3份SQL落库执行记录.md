# 项目档案 3 份 SQL 落库执行记录

- 执行日期：2026-04-09
- 执行人：Codex
- 目标库：`127.0.0.1:3306/finex_db`
- 变更类型：DDL / 权限种子刷新 / 角色权限绑定 / 中文注释校验

## 1. 本次执行对象

按以下顺序执行：

1. `C:\Users\funsir\Desktop\报销系统\backend\sql\init_finance_project_archive.sql`
2. `C:\Users\funsir\Desktop\报销系统\backend\sql\refresh_system_settings_permissions.sql`
3. `C:\Users\funsir\Desktop\报销系统\backend\sql\grant_super_admin_all_permissions.sql`

## 2. 执行前检查

- `SELECT DATABASE()` 确认当前库为 `finex_db`
- `sys_permission`、`sys_role`、`sys_role_permission`、`sys_role` 中 `SUPER_ADMIN` 均存在
- `fin_project_class`、`fin_project_archive` 执行前不存在
- 检查发现两份权限 SQL 存在中文乱码 / 问号文案，已先修复为 `UTF-8 无 BOM` 可读中文后再执行
- `init_finance_project_archive.sql` 已确认表注释、字段注释、索引、外键与 `SET NAMES utf8mb4` 正常

## 3. 备份信息

- 最小备份文件：`C:\Users\funsir\Desktop\报销系统\backend\sql\backup_local\finex_db_project_archive_permissions_20260409_182712.sql`
- 备份范围：
  - `sys_permission`
  - `sys_role`
  - `sys_role_permission`

## 4. 执行结果

- 3 份 SQL 已按顺序执行成功，返回码均为 `0`
- 权限刷新 SQL 已同步修复中文文案，避免将现有权限名称刷成乱码
- `grant_super_admin_all_permissions.sql` 已成功补齐 `SUPER_ADMIN` 对项目档案权限的绑定

## 5. 落库后校验

### 5.1 表结构与中文注释

- 已创建表：
  - `fin_project_class`
  - `fin_project_archive`
- 表注释校验通过：
  - `fin_project_class` = `项目分类表`
  - `fin_project_archive` = `项目档案主目录表`
- 关键字段注释校验通过：
  - `fin_project_class.company_id` = `公司主体编码`
  - `fin_project_class.project_class_code` = `项目分类编码`
  - `fin_project_class.project_class_name` = `项目分类名称`
  - `fin_project_class.status` = `状态：1启用 0停用`
  - `fin_project_archive.company_id` = `公司主体编码`
  - `fin_project_archive.citemcode` = `项目编码`
  - `fin_project_archive.citemname` = `项目名称`
  - `fin_project_archive.citemccode` = `项目分类编码`
  - `fin_project_archive.bclose` = `封存标志：1已封存 0未封存`
  - `fin_project_archive.status` = `状态：1启用 0停用`

### 5.2 权限与角色绑定

- 项目档案权限已存在且名称正确：
  - `finance:archives:projects:view` = `项目档案`
  - `finance:archives:projects:create` = `新增项目档案`
  - `finance:archives:projects:edit` = `编辑项目档案`
  - `finance:archives:projects:disable` = `启停项目档案`
  - `finance:archives:projects:close` = `封存项目档案`
- 路由与排序校验通过：
  - 路径：`/finance/archives/projects`
  - 排序：`4046 / 40461 / 40462 / 40463 / 40464`
- `SUPER_ADMIN` 已绑定上述 5 个项目档案权限

### 5.3 防乱码与数据状态

- `sys_permission.permission_name` 乱码扫描结果：`bad_count = 0`
- 新建表初始数据状态：
  - `fin_project_class` 记录数：`0`
  - `fin_project_archive` 记录数：`0`

## 6. 结论

- 本次项目档案 3 份 SQL 已在本机 `finex_db` 安全落库完成
- 表结构、中文注释、权限种子、`SUPER_ADMIN` 绑定均已完成校验
- 当前数据库已具备 `财务管理 > 会计档案 > 项目档案` 首版真实功能所需的基础落库条件
