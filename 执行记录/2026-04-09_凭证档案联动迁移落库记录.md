# 凭证档案联动迁移落库记录

- 执行日期：2026-04-09 23:25:30 - 23:31:xx
- 执行人：Codex
- 目标数据库：127.0.0.1:3306/finex_db
- 变更类型：DDL / DML / 索引补齐 / 编码规则迁移
- 执行脚本：`backend/sql/migrate_finance_voucher_archive_linkage.sql`

## 执行前核查

- `SELECT DATABASE()`：`finex_db`
- `fin_project_class`：1 条
- `fin_project_archive`：1 条
- `gl_accvouch`：0 条
- 项目分类现状：`SMOKE_CLASS`，长度 11
- 项目档案现状：`SMOKE_PROJECT`，长度 13，分类 `SMOKE_CLASS`
- `gl_accvouch.ccode_name`：不存在
- `gl_accvouch` 本轮 7 个复合索引：不存在

## 最小备份

- `执行记录/2026-04-09_232530_gl_accvouch_backup.sql`
- `执行记录/2026-04-09_232530_fin_project_class_backup.sql`
- `执行记录/2026-04-09_232530_fin_project_archive_backup.sql`

## 执行过程

- 首次执行迁移脚本时，命中 `gl_accvouch` 与临时映射表字符集排序规则冲突，报错：`Illegal mix of collations`
- 对迁移脚本补充了 `gl_accvouch.company_id / citem_class / citem_id` 关联处的显式 `COLLATE utf8mb4_unicode_ci`
- 第二次执行迁移脚本时，命中 `fin_project_archive -> fin_project_class` 外键约束，报错：修改 `project_class_code` 前需先处理外键
- 对迁移脚本补充了：
  - 变更前动态删除 `fk_fin_project_archive_class`
  - 变更后动态重建 `fk_fin_project_archive_class`
- 修正后再次整文件执行成功

## 落库结果

- `gl_accvouch` 已新增 `ccode_name varchar(255)`，注释为 `科目名称`
- `gl_accvouch` 已补齐 7 个公司维度复合索引
- `fin_project_class.project_class_code` 已迁移为 2 位数字文本
- `fin_project_archive.citemcode` 已迁移为 6 位数字文本
- `fin_project_archive.citemccode` 已同步为 2 位数字文本分类编码
- 已补齐 3 条格式约束：
  - `ck_fin_project_class_code_format`
  - `ck_fin_project_archive_code_format`
  - `ck_fin_project_archive_class_format`

## 验收结果

- 项目分类：`01 / 冒烟分类`
- 项目档案：`000001 / 冒烟项目 / 01`
- `gl_accvouch` 当前数据量：0，未产生历史凭证引用修正风险
- 本次验收通过的关键检查：
  - `SHOW COLUMNS FROM gl_accvouch LIKE 'ccode_name'`
  - `SHOW INDEX FROM gl_accvouch ...`
  - `information_schema.COLUMNS` 中文注释检查
  - `information_schema.TABLE_CONSTRAINTS` 检查 3 条 `ck_fin_project_*` 约束

## 备注

- 本次执行不仅完成了本机 `finex_db` 落库，也同步修正了迁移脚本在当前库排序规则与外键场景下的可执行性问题，后续再次执行具备更好的幂等与兼容性。
