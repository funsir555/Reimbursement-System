# 数据库执行记录：员工多部门归属关系初始化

- 执行日期：2026-05-03 22:12:09
- 执行人：Codex
- 目标库：127.0.0.1:3306/finex_db
- 目标对象：sys_user_department_rel
- 变更类型：DDL / DML
- 风险级别：二级

## 执行前校验 SQL

```sql
SELECT DATABASE() AS db_name;
SELECT TABLE_NAME
FROM information_schema.TABLES
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME IN ('sys_user', 'sys_department', 'sys_user_department_rel')
ORDER BY TABLE_NAME;
SELECT COUNT(*) AS user_count FROM sys_user;
SELECT COUNT(*) AS dept_count FROM sys_department;
SELECT COUNT(*) AS users_with_dept FROM sys_user WHERE dept_id IS NOT NULL;
SELECT COUNT(*) AS orphan_user_dept_count
FROM sys_user u
LEFT JOIN sys_department d ON d.id = u.dept_id
WHERE u.dept_id IS NOT NULL
  AND d.id IS NULL;
SELECT COUNT(*) AS rel_table_exists
FROM information_schema.TABLES
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME = 'sys_user_department_rel';
```

执行前结果：

- `sys_user` 已存在，员工数：137
- `sys_department` 已存在，部门数：50
- `sys_user_department_rel` 不存在
- `sys_user.dept_id IS NOT NULL` 员工数：135
- `sys_user.dept_id` 指向不存在部门的异常数据：0

## 正式执行脚本

- 文件：`backend/sql/migrate_user_department_relation.sql`
- 执行方式：PowerShell 读取 SQL 后通过管道输入 `mysql`
- 执行说明：未执行 `backend/sql/init.sql`

```sql
USE finex_db;

SET NAMES utf8mb4;

CREATE TABLE IF NOT EXISTS sys_user_department_rel (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '员工部门归属关系ID',
    user_id BIGINT NOT NULL COMMENT '员工ID',
    dept_id BIGINT NOT NULL COMMENT '部门ID',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    CONSTRAINT uk_sys_user_department_rel_user_dept UNIQUE (user_id, dept_id),
    KEY idx_sys_user_department_rel_user_id (user_id),
    KEY idx_sys_user_department_rel_dept_id (dept_id),
    CONSTRAINT fk_sys_user_department_rel_user_id FOREIGN KEY (user_id) REFERENCES sys_user(id),
    CONSTRAINT fk_sys_user_department_rel_dept_id FOREIGN KEY (dept_id) REFERENCES sys_department(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='员工部门归属关系表';

INSERT IGNORE INTO sys_user_department_rel (user_id, dept_id, created_at, updated_at)
SELECT id, dept_id, NOW(), NOW()
FROM sys_user
WHERE dept_id IS NOT NULL;
```

## 备份信息

- 备份方式：`mysqldump`
- 备份位置：`执行记录/db-backups/20260503_221123_user_department_relation/finex_db_sys_user_sys_department_before_user_department_relation.sql`
- 备份对象：`sys_user`、`sys_department`
- 备份文件大小：45867 bytes
- 说明：执行前 `sys_user_department_rel` 不存在，因此未备份该表。

## 回滚方案

如需回滚本次初始化：

```sql
DROP TABLE IF EXISTS sys_user_department_rel;
```

说明：本次未修改 `sys_user`、`sys_department` 原始数据；如发现上游基础数据异常，可使用备份文件恢复 `sys_user`、`sys_department`。

## 执行后验证 SQL

```sql
SELECT COUNT(*) AS rel_table_exists
FROM information_schema.TABLES
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME = 'sys_user_department_rel';

SELECT COUNT(*) AS rel_count FROM sys_user_department_rel;
SELECT COUNT(*) AS users_with_dept FROM sys_user WHERE dept_id IS NOT NULL;

SELECT COUNT(*) AS missing_rel_count
FROM sys_user u
LEFT JOIN sys_user_department_rel r
  ON r.user_id = u.id AND r.dept_id = u.dept_id
WHERE u.dept_id IS NOT NULL
  AND r.id IS NULL;

SELECT COUNT(*) AS orphan_rel_user_count
FROM sys_user_department_rel r
LEFT JOIN sys_user u ON u.id = r.user_id
WHERE u.id IS NULL;

SELECT COUNT(*) AS orphan_rel_dept_count
FROM sys_user_department_rel r
LEFT JOIN sys_department d ON d.id = r.dept_id
WHERE d.id IS NULL;

SELECT INDEX_NAME, COLUMN_NAME, NON_UNIQUE
FROM information_schema.STATISTICS
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME = 'sys_user_department_rel'
ORDER BY INDEX_NAME, SEQ_IN_INDEX;

SELECT CONSTRAINT_NAME, REFERENCED_TABLE_NAME
FROM information_schema.KEY_COLUMN_USAGE
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME = 'sys_user_department_rel'
  AND REFERENCED_TABLE_NAME IS NOT NULL
ORDER BY CONSTRAINT_NAME;
```

验证结果：

- `sys_user_department_rel` 表存在：1
- 关系表行数：135
- 历史 `dept_id` 非空员工数：135
- 未回填历史部门关系数：0
- 关系表孤儿用户引用数：0
- 关系表孤儿部门引用数：0
- 唯一键存在：`uk_sys_user_department_rel_user_dept`
- 外键存在：`fk_sys_user_department_rel_user_id`、`fk_sys_user_department_rel_dept_id`

## 结果结论

- 实际影响行数：新增 `sys_user_department_rel` 表，回填 135 条员工部门归属关系。
- 是否成功：成功。
- 是否影响前端：预期支持系统设置中心员工多部门归属展示和维护。
- 是否影响后端：新增关系表满足多部门归属持久化需求；未修改原 `sys_user.dept_id`。
- 后续待办：建议启动后端并进入系统设置中心员工管理页做一次人工验收。
