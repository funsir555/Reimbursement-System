# 数据库执行记录：期末结转增量脚本执行

- 执行日期：2026-05-19 09:17:44
- 执行人：Codex
- 目标库：127.0.0.1:3306/finex_db
- 目标对象：`gl_period_transfer_rule`、`gl_period_transfer_rule_line`、`gl_period_transfer_run`、`gl_period_transfer_run_detail`、`gl_period_transfer_voucher_link`、期末结转权限点
- 变更类型：DDL / 初始化补数
- 风险级别：一级

## 执行前校验 SQL

```sql
SELECT COUNT(*) AS PRECHECK_TABLES
FROM information_schema.TABLES
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME IN (
    'gl_period_transfer_rule',
    'gl_period_transfer_rule_line',
    'gl_period_transfer_run',
    'gl_period_transfer_run_detail',
    'gl_period_transfer_voucher_link'
  );

SELECT COUNT(*) AS PRECHECK_PERMISSIONS
FROM sys_permission
WHERE permission_code IN (
  'finance:general_ledger:period_transfer:view',
  'finance:general_ledger:period_transfer:manage',
  'finance:general_ledger:period_transfer:generate'
);
```

- 执行前结果：
  - `PRECHECK_TABLES = 0`
  - `PRECHECK_PERMISSIONS = 0`

## 正式执行脚本

- 文件：`backend/sql/migrate_finance_period_transfer.sql`
- 执行方式：`mysql.exe` 命令行直连 `127.0.0.1:3306/finex_db`
- 幂等性验证：同一脚本连续执行 2 次，第二次执行不报错

## 预计影响范围

- 新增 5 张总账期末结转相关业务表
- 新增或刷新 3 个权限点
- 不修改既有总账运行表结构
- 不删除历史业务数据

## 备份信息

- 备份方式：本次为新增对象型一级变更，未涉及历史数据修复或物理删除，执行前未额外导出业务数据备份
- 备份位置：不适用

## 回滚方案

```sql
-- 本次未执行回滚；若需回滚，必须在确认无业务数据写入后单独评估执行。
DELETE FROM sys_permission
WHERE permission_code IN (
  'finance:general_ledger:period_transfer:view',
  'finance:general_ledger:period_transfer:manage',
  'finance:general_ledger:period_transfer:generate'
);

DROP TABLE IF EXISTS gl_period_transfer_voucher_link;
DROP TABLE IF EXISTS gl_period_transfer_run_detail;
DROP TABLE IF EXISTS gl_period_transfer_run;
DROP TABLE IF EXISTS gl_period_transfer_rule_line;
DROP TABLE IF EXISTS gl_period_transfer_rule;
```

- 回滚说明：
  - 上述回滚 SQL 仅适用于“确认尚无真实业务数据写入”的前提。
  - 一旦已有结转规则、运行记录或关联凭证写入，不得直接执行物理回滚，需先专项评估数据保留与恢复方案。

## 执行后验证 SQL

```sql
SELECT TABLE_NAME, TABLE_COMMENT
FROM information_schema.TABLES
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME IN (
    'gl_period_transfer_rule',
    'gl_period_transfer_rule_line',
    'gl_period_transfer_run',
    'gl_period_transfer_run_detail',
    'gl_period_transfer_voucher_link'
  )
ORDER BY TABLE_NAME;

SELECT COUNT(*) AS POSTCHECK_TABLES
FROM information_schema.TABLES
WHERE TABLE_SCHEMA = 'finex_db'
  AND TABLE_NAME IN (
    'gl_period_transfer_rule',
    'gl_period_transfer_rule_line',
    'gl_period_transfer_run',
    'gl_period_transfer_run_detail',
    'gl_period_transfer_voucher_link'
  );

SELECT permission_code
FROM sys_permission
WHERE permission_code IN (
  'finance:general_ledger:period_transfer:view',
  'finance:general_ledger:period_transfer:manage',
  'finance:general_ledger:period_transfer:generate'
)
ORDER BY permission_code;

SELECT COUNT(*) AS POSTCHECK_PERMISSIONS
FROM sys_permission
WHERE permission_code IN (
  'finance:general_ledger:period_transfer:view',
  'finance:general_ledger:period_transfer:manage',
  'finance:general_ledger:period_transfer:generate'
);
```

## 结果结论

- 实际影响结果：
  - `POSTCHECK_TABLES = 5`
  - `POSTCHECK_PERMISSIONS = 3`
- 表注释校验结果：
  - `gl_period_transfer_rule` -> `总账期末结转规则表`
  - `gl_period_transfer_rule_line` -> `总账期末结转规则明细表`
  - `gl_period_transfer_run` -> `总账期末结转运行表`
  - `gl_period_transfer_run_detail` -> `总账期末结转运行明细表`
  - `gl_period_transfer_voucher_link` -> `总账期末结转凭证关联表`
- 权限点校验结果：
  - `finance:general_ledger:period_transfer:view`
  - `finance:general_ledger:period_transfer:manage`
  - `finance:general_ledger:period_transfer:generate`
- 是否成功：成功
- 是否影响前端：已完成页面冒烟验证，`/finance/home` 与 `/finance/general-ledger/period-transfer` 可正常打开
- 是否影响后端：`auth-service`、`gateway` 本次实测期间可正常提供接口
- 风险结论：
  - 脚本已通过重复执行验证，满足旧库增量幂等要求
  - 本次未触发历史数据删除、乱码注释或结构漂移问题
- 后续待办：
  - 如需继续做“期末结转生成凭证”深链路验收，建议补一轮带样例规则与生成记录的业务回归
