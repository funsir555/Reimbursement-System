Phase 1 governance note:

- Put new API contracts and request wrappers under this directory by domain.
- Keep `src/api/index.ts` as a compatibility layer during the migration.
- Do not add new interface definitions or new API groups back into `src/api/index.ts`.
