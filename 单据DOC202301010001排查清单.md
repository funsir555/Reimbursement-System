# 单据 DOC202301010001 不在待推送列表排查清单

## 排查位置
文件：`ExpenseVoucherPushDomainSupport.java` 的 `getPushDocuments` 方法

## 可能的原因（按代码执行顺序）

### 1. 单据不在已审批单据列表中
**代码位置**：第62行 `listApprovedDocuments()`
**检查项**：
- 单据状态是否为以下之一：
  - `APPROVED` (已审批)
  - `COMPLETED` (已完成)
  - `PENDING_PAYMENT` (待支付)
  - `PAYMENT_COMPLETED` (支付完成)
  - `PAYMENT_FINISHED` (支付结束)

**排查方法**：
```sql
SELECT document_code, status 
FROM pm_document_instance 
WHERE document_code = 'DOC202301010001';
```

---

### 2. 模板类型不符合（新增的过滤条件）
**代码位置**：第63-66行
**检查项**：
- 模板类型必须是 `report`（员工费用类）或 `contract`（企业往来类）
- 不能是 `application`（事项申请类）或 `loan`（借款单）

**排查方法**：
```sql
SELECT document_code, template_type, template_code, template_name 
FROM pm_document_instance 
WHERE document_code = 'DOC202301010001';
```

---

### 3. 公司ID不匹配（如果有companyId筛选条件）
**代码位置**：第67-70行
**检查项**：
- 单据的公司ID是否能正确解析
- 是否匹配当前用户的公司筛选条件

**排查方法**：
```sql
-- 查看单据的form_data_json，找到payment-company字段
SELECT document_code, form_data_json 
FROM pm_document_instance 
WHERE document_code = 'DOC202301010001';

-- 查看提交人的公司ID
SELECT d.document_code, u.company_id, u.username 
FROM pm_document_instance d
LEFT JOIN sys_user u ON d.submitter_user_id = u.user_id
WHERE d.document_code = 'DOC202301010001';
```

---

### 4. 模板代码不匹配（如果有templateCode筛选条件）
**代码位置**：第71-73行
**检查项**：
- 单据的模板代码是否匹配筛选条件

**排查方法**：
```sql
SELECT document_code, template_code 
FROM pm_document_instance 
WHERE document_code = 'DOC202301010001';
```

---

### 5. 关键词不匹配（如果有keyword筛选）
**代码位置**：第74-76行
**检查项**：
- 单据号、模板名称、提交人姓名、单据标题是否包含搜索关键词

---

### 6. 日期范围不匹配（如果有日期筛选）
**代码位置**：第73行 `matchesDateRange`
**检查项**：
- 单据的业务日期（finished_at、updated_at 或 created_at）是否在筛选的日期范围内

**排查方法**：
```sql
SELECT document_code, finished_at, updated_at, created_at 
FROM pm_document_instance 
WHERE document_code = 'DOC202301010001';
```

---

### 7. 推送状态不匹配（如果有pushStatus筛选）
**代码位置**：第77-80行
**检查项**：
- 如果单据已经推送成功（SUCCESS），但筛选条件是 UNPUSHED 或 FAILED，则不会显示

**排查方法**：
```sql
SELECT document_code, push_status, error_message, voucher_no, pushed_at 
FROM exp_voucher_push_document 
WHERE document_code = 'DOC202301010001';
```

---

## 快速诊断SQL

运行以下SQL获取完整诊断信息：

```sql
SELECT 
    d.document_code AS '单据号',
    d.template_code AS '模板代码',
    d.template_name AS '模板名称',
    d.template_type AS '模板类型',
    d.status AS '单据状态',
    d.finished_at AS '审批完成时间',
    d.total_amount AS '总金额',
    u.company_id AS '提交人公司ID',
    u.username AS '提交人',
    p.push_status AS '推送状态',
    p.error_message AS '失败原因',
    p.voucher_no AS '凭证号',
    p.pushed_at AS '推送时间',
    CASE 
        WHEN d.status NOT IN ('APPROVED', 'COMPLETED', 'PENDING_PAYMENT', 'PAYMENT_COMPLETED', 'PAYMENT_FINISHED') 
        THEN '❌ 单据状态不符合'
        WHEN d.template_type NOT IN ('report', 'contract') AND d.template_type IS NOT NULL
        THEN '❌ 模板类型不符合（事项申请类不能映射凭证）'
        WHEN d.template_type IS NULL OR d.template_type = ''
        THEN '⚠️  模板类型为空，默认视为report'
        ELSE '✅ 符合基本条件'
    END AS '诊断结果'
FROM pm_document_instance d
LEFT JOIN sys_user u ON d.submitter_user_id = u.user_id
LEFT JOIN exp_voucher_push_document p ON d.document_code = p.document_code
WHERE d.document_code = 'DOC202301010001';
```

---

## 最可能的原因

根据代码逻辑，最常见的原因是：

1. **单据状态不是已审批状态** - 占70%
2. **模板类型是事项申请类（application）** - 占20%（刚刚新增的过滤条件）
3. **推送状态筛选导致** - 占5%
4. **其他筛选条件** - 占5%

---

## 下一步操作

1. 在数据库中运行上面的"快速诊断SQL"
2. 根据"诊断结果"列的提示，定位具体问题
3. 如果需要调整代码，请告知具体情况
